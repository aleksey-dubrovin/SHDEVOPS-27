---
# Yandex Cloud CLI settings
yc_cli_path: "yc"

# Validation settings (будут переопределены из all.yml)
validation:
  checks:
    yc_cli_installed: true
    yc_token_valid: true
    folder_exists: true
    subnet_exists: true
    image_exists: true
  fail_fast: true